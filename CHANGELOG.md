# 变更记录

## 1.1.0 — 2026-09-19

- 新增 `sdram_disable()`，停用软件访问并保留诊断信息。
- 自检数据不匹配时自动停用实例；修复后需重新初始化。
- 新增 `sdram_test_ex()` 及可选进度回调，支持主动取消。
- 保留 `sdram_test()`；新增错误码追加在原枚举末尾，原状态码数值不变。
- 回调通知不改变整区写入后再校验的测试顺序。
- 测试运行器支持指定 HAL 工程、头文件目录、编译器和输出目录，并固定 Python 依赖版本。
- CMake 显式要求 C11。硬件验证仍待完成。

## 1.0.0 — 2026-09-19

- 初始软件基线：上电命令、刷新计算、16 位读写/填充、破坏性自检。
- 组件名为 `stm_sdram`，函数统一为 `sdram_*`；此前开发期间的 `stm_sdram_*` 函数名已替换。
- 注释采用简短中文、公开接口 Doxygen 及结构体字段行尾说明。
